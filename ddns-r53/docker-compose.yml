version: '3.5'
services:
  ddns-r53:
    container_name: ddns-r53
    image: aaronfagan/ddns-r53
    env_file:
      - ./config.env
    privileged: true
    restart: always
networks:
  default:
    external:
      name: homeserver
